# 服务器配置
PORT=8080

# 环境
GO_ENV=development

# 数据库配置（示例）
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=example

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h
